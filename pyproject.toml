[project]
name = "fluorescence-simulation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Otto Hanski", email = "otto.hanski@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.4.4",
    "tqdm>=4.67.3",
    "typed-argument-parser>=1.12.0",
]


[project.scripts]
fluorescence-simulation = "fluorescence_simulation.simulation:main"


[build-system]
requires = ["uv_build>=0.11.7,<0.12.0"]
build-backend = "uv_build"
